@using SummerCamp.Models
@model IEnumerable<CoachViewModel>
@{
    ViewData["Title"] = "Coaches";
}
<h1 class="centered-title">Coaches</h1>
<a asp-controller="Coaches" asp-action="Add" class="btn btn-primary">Add coach</a>
<div class="text-center">
    <div class="table-responsive">
        <table class="table table-bordered table-condensed table-striped table-hover sortable">
            @if (Model.Count() == 0)
            {
                <tr>
                    <td colspan="6">No Records found.</td>
                </tr>
            }
            else
            {
                <thead class="thead-dark">
                    <tr>
                        <th>Full Name</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.FullName</td>
                            <td>
                                <a asp-action="Edit" asp-controller="Coaches" asp-route-coachId="@item.Id" class="btn btn-primary">
                                    Edit
                                </a>
                                <a asp-action="Delete" asp-controller="Coaches" asp-route-coachId="@item.Id" class="btn btn-primary">
                                    Delete
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            }
        </table>
    </div>
</div>

