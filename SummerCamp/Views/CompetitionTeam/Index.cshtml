@using SummerCamp.Models
@model IEnumerable<CompetitionTeamViewModel>
@{
    ViewData["Title"] = "CompetitionTeams";
}
<h1 class="centered-title">CompetitionTeams</h1>
<a asp-controller="CompetitonTeam" asp-action="Add" class="btn btn-primary">Add team to competition</a>
<div class="text-center">
    <div class="table-responsive">
        <table class="table table-bordered table-condensed table-striped table-hover sortable">
            @if (Model.Count() == 0)
            {
                <tr>
                    <td colspan="6">No Records found.</td>
                </tr>
            }
            else
            {
                <thead class="thead-dark">
                    <tr>
                        <th>Team</th>
                        <th>Competition</th>
                        <th>Points</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Team</td>
                            <td>@item.Competition</td>
                            <td>@item.TotalPoints</td>
                            <td>
                                <a asp-action="Edit" asp-controller="CompetitionTeam" asp-route-competitionteamId="@item.Id" class="btn btn-primary">
                                    Edit
                                </a>
                                <a asp-action="Delete" asp-controller="CompetitionTeam" asp-route-competitionteamId="@item.Id" class="btn btn-primary">
                                    Delete
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            }
        </table>
    </div>
</div>

