@using SummerCamp.Models
@model IEnumerable<TeamViewModel>
@{
    ViewData["Title"] = "Teams";
}
<h1 class="centered-title">Teams</h1>
<a asp-controller="Team" asp-action="Add" class="btn btn-primary">Add team</a>
<div class="text-center">
    <div class="table-responsive">
        <table class="table table-bordered table-condensed table-striped table-hover sortable">
            @if (Model.Count() == 0)
            {
                <tr>
                    <td colspan="6">No Records found.</td>
                </tr>
            }
            else
            {
                <thead class="thead-dark">
                    <tr>
                        <th>Name</th>
                        <th>NickName</th>
                        <th>CoachId</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.NickName</td>
                            <td>@item.CoachId</td>
                            <td>
                                <a asp-action="Edit" asp-controller="Team" asp-route-teamId="@item.Id" class="btn btn-primary">
                                    Edit
                                </a>
                                <a asp-action="Show" asp-controller="Team" asp-route-teamId="@item.Id" class="btn btn-primary">
                                    Show
                                </a>
                                <a asp-action="Delete" asp-controller="Team" asp-route-teamId="@item.Id" class="btn btn-primary">
                                    Delete
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            }
        </table>
    </div>
</div>

